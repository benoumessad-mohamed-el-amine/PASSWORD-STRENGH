"""
Models for checker app.

This app doesn't store passwords for security reasons.
All password analysis is done in memory and results are not persisted.
"""
from django.db import models

# No models needed - we don't store passwords!
